/**
 * Sets that will be later transformed into API-compliant format
 */
export type ActiveSets = {
  reps: string;
  value: string; // store temporarily as string to allow users to enter decimal points
};
